1.Make a second repo on the collabortators comouter and merge them together
2. developers make branches to keep their work organized

3.Git is a command line that allows you to track and manage changes to your code and github is a data based platform to help you store repos.

3. its wrong because git add . should be first.the correct command sequence should be
git add . 
git commit -m "saving work 
git push